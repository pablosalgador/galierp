<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">
  <!-- Sidebar - Brand -->
  <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{path("index")}}">
    <div class="sidebar-brand-icon rotate-n-15">
      <i class="fas fa-clipboard"></i>
    </div>
    <div class="sidebar-brand-text mx-3">GaliERP <sup>1.0a</sup></div>
  </a>
  <!-- Divider -->
  <hr class="sidebar-divider my-0">
  <li class="nav-item">
    <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#navclientes" aria-expanded="true" aria-controls="navclientes">
      <i class="fas fa-fw fa-user-tie"></i>
      <span>Clientes</span>
    </a>
    <div id="navclientes" class="collapse" aria-labelledby="navclientes" data-parent="#accordionSidebar">
      <div class="bg-white py-2 collapse-inner rounded">
        <h6 class="collapse-header">{% trans %}Gestión de clientes{% endtrans %}</h6>
        <a class="collapse-item" href="{{path("listaclientes")}}"><i class="fas fa-users fa-fw"></i> &nbsp;{% trans %}Ver Lista{% endtrans %}</a>
        <a class="collapse-item" href="{{path("nuevocliente")}}"><i class="fas fa-user-plus fa-fw"></i> &nbsp;{% trans %}Añadir Nuevo{% endtrans %}</a>
      </div>
    </div>
  </li>

  <!-- Divider -->
  <hr class="sidebar-divider">

  <!-- Sidebar: Administrador -->
  {% if is_granted('ROLE_ADMIN') %}
    {% include 'nav/navbar-admin.html.twig' %}
  {% endif %}
  <!-- Fin Sidebar: Administrador -->

  <!-- Sidebar: CRM -->
  {% if is_granted('ROLE_CRM') %}
    {% include 'nav/navbar-crm.html.twig' %}
  {% endif %}
  <!-- End Sidebar -->

  <!-- Sidebar (Compartida por fiscal y crm )-->
  {% if is_granted('ROLE_CRM') or is_granted('ROLE_FISCAL') %}
  <li class="nav-item">
    <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#navpresupuestos" aria-expanded="true" aria-controls="navpresupuestos">
      <i class="fas fa-fw fa-file-invoice"></i>
      <span>{% trans %}Presupuestos{% endtrans %}</span>
    </a>
    <div id="navpresupuestos" class="collapse" aria-labelledby="navpresupuestos" data-parent="#accordionSidebar">
      <div class="bg-white py-2 collapse-inner rounded">
        <h6 class="collapse-header">{% trans %}Presupuestos{% endtrans %}</h6>
        <a class="collapse-item" href="{{path('lista_presupuestos')}}"><i class="fas fa-bell fa-file-invoice"></i> &nbsp;{% trans %}Ver Lista{% endtrans %}</a>
        <a class="collapse-item" href="{{path('nuevo_presupuesto')}}"><i class="fas fa-bell fa-file-medical"></i> &nbsp;{% trans %}Nuevo{% endtrans %}</a>
      </div>
    </div>
  </li>
  <!-- Divider -->
  <hr class="sidebar-divider d-none d-md-block">
  {% endif %}
  <!-- Fin Sidebar (Compartida por fiscal y crm )-->

  <!-- Sidebar: Fiscal -->
  {% if is_granted('ROLE_FISCAL') %}
    {% include 'nav/navbar-fiscal.html.twig' %}
  {% endif %}
  <!-- Fin sidebar: Fiscal -->

  <!-- Sidebar Toggler (Sidebar) -->
  <div class="text-center d-none d-md-inline">
    <button class="rounded-circle border-0" id="sidebarToggle"></button>
  </div>

</ul>
